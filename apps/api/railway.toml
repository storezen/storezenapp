[railway]
  runtime = "nodejs"
  nodeVersion = "22"

[build]
builder = "RAILPACK"
buildCommand = "pnpm install && pnpm run build"

[deploy]
startCommand = "pnpm run start"
healthcheckPath = "/api/healthz"