[railway]
  runtime = "nodejs"
  nodeVersion = "22"

[build]
builder = "NIXPACKS"
buildCommand = "cd apps/api && pnpm install && pnpm run build"

[deploy]
startCommand = "cd apps/api && node dist/index.mjs"
healthcheckPath = "/api/healthz"