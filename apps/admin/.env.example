# Platform admin (Next.js) — copy to apps/admin/.env.local

# Must include `/api` — the API app mounts routes at http://host:port/api
NEXT_PUBLIC_API_URL=http://localhost:3000/api
NEXT_PUBLIC_ADMIN_EMAIL=admin@storepk.com
