# Storezen API - Railway Deployment
# Root Directory: apps/api

[build]
builder = "RAILPACK"
buildCommand = "cd ../.. && pnpm install && pnpm --filter @storepk/api build"
nodeVersion = "22"

[deploy]
startCommand = "node ./apps/api/dist/index.mjs"
healthcheckPath = "/health"
restart = "on-failure"