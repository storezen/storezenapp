{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:web": "turbo run build --filter=@workspace/pk-store",
    "build:dashboard": "turbo run build --filter=@workspace/dashboard",
    "build:admin": "turbo run build --filter=@workspace/admin",
    "build:api": "turbo run build --filter=@workspace/api-server",
    "db:migrate": "cd apps/api && pnpm drizzle-kit migrate",
    "db:seed": "cd apps/api && pnpm ts-node src/db/seed.ts",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./apps/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.1",
    "ts-node": "^10.9.2",
    "turbo": "^2.5.8",
    "typescript": "~5.9.2"
  }
}
