# Monorepo API — Railway Root directory = apps/api

[build]
builder = "RAILPACK"
buildCommand = "cd ../.. && pnpm install && pnpm --filter @storezen/api build"

[deploy]
startCommand = "node ./apps/api/dist/index.mjs"
healthcheckPath = "/api/healthz"