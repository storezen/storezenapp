[build]
builder = "nixpacks"
buildCommand = "pnpm install --no-frozen-lockfile && pnpm run build:api"
startCommand = "node apps/api/dist/index.mjs"

[node]
version = "20"

[Etcd]
URL = "${DATABASE_URL}"