# Monorepo API — Railway **Root directory** = `apps/api`

[build]
builder = "RAILPACK"
buildCommand = "NODE_OPTIONS='--max-old-space-size=4096' node ./build.mjs"

[deploy]
startCommand = "node ./dist/index.mjs"
healthcheckPath = "/api/healthz"
