SEO & Meta Tags
"""
Add proper SEO meta tags and Open Graph tags. Requirements:

For each page, set dynamic meta tags:

Home: title="SmartWear — Trending Fashion & Lifestyle Products", description from config
Catalog: title="Shop All Products — SmartWear"
Product Detail: title="{product_name} — SmartWear", description=product description (truncated to 160 chars), og:image=product image
Contact: title="Contact Us — SmartWear"


Open Graph tags for social sharing (Facebook, WhatsApp preview):

og:title, og:description, og:image, og:url, og:type
For products: og:type="product", product:price:amount, product:price:currency=PKR


Twitter Card tags:

twitter:card="summary_large_image"
twitter:title, twitter:description, twitter:image


Add structured data (JSON-LD) for products:

@type: Product
name, description, image, offers (price, currency, availability)


Ensure all meta tags update when navigating between pages (if using React Router or similar).
Add a sitemap.xml and robots.txt if possible.
"""

BONUS 2 — PWA (Progressive Web App) Setup
"""
Convert SmartWear into a Progressive Web App (PWA) so customers can install it on their phone. Requirements:

Create a manifest.json:

name: "SmartWear"
short_name: "SmartWear"
theme_color: match the dark theme primary color
background_color: match the dark background
display: "standalone"
start_url: "/"
icons: Create simple SmartWear logo icons at 192x192 and 512x512 (use a colored "SW" text on dark background as placeholder)


Create a basic service worker that:

Caches the app shell (HTML, CSS, JS)
Caches product images for offline viewing
Shows a "You're offline" message if network is unavailable
Updates cache when new version is available


Add "Install App" prompt:

Show a banner at the bottom of the page: "📱 Install SmartWear app for the best experience" with an "Install" button
Only show on mobile browsers that support PWA
Dismiss button that hides for 7 days (localStorage)


Add to head: <link rel="manifest" href="/manifest.json"> and the Apple touch icon meta tags for iOS.
"""

BONUS 3 — Performance & Loading Optimization
"""
Optimize the store's loading performance. Requirements:

Image Optimization:

Add lazy loading to all product images (loading="lazy")
Add image placeholders/skeleton loading: Show a gray pulsing placeholder while images load
Use srcset for responsive images if Unsplash is used (append ?w=200 for thumbnails, ?w=600 for detail)


Code Splitting:

Lazy load the Product Detail page component
Lazy load the Cart page component
Lazy load the Admin dashboard (it should definitely not load for regular users)


Skeleton Loading Screens:

Home page: Show skeleton cards while products load (gray animated rectangles)
Product Detail: Show skeleton layout for image + text areas
Catalog: Show skeleton grid while filtering/sorting


Optimize animations:

Use will-change for animated elements
Use CSS containment (contain: layout style)
Remove or simplify animations on low-performance devices (use prefers-reduced-motion media query)


Add page transition animations:

Smooth fade between pages
Slide-in for product detail page


Minimize re-renders — memoize expensive components and filter/sort operations.
"""