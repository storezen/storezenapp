# features

Create one folder per feature domain, for example:

- `features/cart`
- `features/checkout`
- `features/admin`
- `features/tracking`

Each feature should contain its own UI, hooks, and service logic when possible.
