# Railway config for API
[railway]
  runtime = "nodejs"

[build]
builder = "RAILPACK"
buildCommand = "pnpm install && pnpm run build:skip-typecheck"

[deploy]
startCommand = "pnpm start"
healthcheckPath = "/api/healthz"
