[railway]
  runtime = "nodejs"
  nodeVersion = "22"

[build]
  builder = "nixpacks"
  buildCommand = "cd apps/api && pnpm install --no-frozen-lockfile && pnpm run build"
  startCommand = "node apps/api/dist/index.mjs"

[deploy]
  healthcheckPath = "/api/healthz"