# app

Use this folder for application composition only:

- route tree / router wiring
- providers bootstrap (query, theme, cart, wishlist)
- top-level layout shells
- app startup orchestration

Avoid feature business logic here. Place that logic in `features/`.
