{
  "version": 2,
  "buildCommand": "cd apps/web && pnpm install --no-frozen-lockfile && pnpm run build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "cd apps/web && pnpm install --no-frozen-lockfile",
  "framework": "nextjs"
}