{
  "version": 2,
  "buildCommand": "pnpm install --filter=@storepk/web --frozen-lockfile && pnpm --filter=@storepk/web run build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install --filter=@storepk/web",
  "framework": "nextjs"
}